If you dont like mixing your CSS and TSX files you can group them in folders, like this one (ProductCard). This is a container for our component for these files.

We can use the same technique with our pages. We can use CSS modules to define styles that are local to that particular page.